# ArgoCD Application patch for per-cluster dashboard deployment.
# Reference this from your cluster's root kustomization.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhoai-openshell-dashboard
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: rhoai-platform
  source:
    repoURL: https://github.com/rrbanda/ai-platforms.git
    targetRevision: main
    # Point to the cluster-specific overlay:
    path: redhat/rhoai/v3.5/overlays/REPLACE_CLUSTER_NAME/dashboard
  destination:
    server: https://kubernetes.default.svc
    namespace: openshell-dashboard
  ignoreDifferences:
    - group: route.openshift.io
      kind: Route
      jsonPointers:
        - /spec/host
        - /metadata/annotations
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
  syncPolicy:
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
