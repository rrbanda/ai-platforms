apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - gateway.yaml
  - rbac.yaml
  - mcpgatewayextension.yaml
  # MCPServer + config now managed by Helm chart (03b-mcp-server-helm app)
