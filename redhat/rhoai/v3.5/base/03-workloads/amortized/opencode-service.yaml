apiVersion: v1
kind: Service
metadata:
  name: opencode
  namespace: amortized
  labels:
    app: amortized
    component: opencode
spec:
  type: ClusterIP
  selector:
    app: amortized
    component: opencode
  ports:
    - name: http
      port: 4096
      targetPort: http
