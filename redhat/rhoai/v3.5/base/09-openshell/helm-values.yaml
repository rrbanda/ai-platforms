# OpenShell Helm values -- OpenShift base defaults.
# mTLS is the default; access via port-forward or ingress.
# Ref: OpenShell on OpenShift quick reference.
podSecurityContext:
  fsGroup: null

securityContext:
  runAsUser: null

server:
  auth:
    allowUnauthenticatedUsers: true
  # Operator mode: each workspace maps 1:1 to a pre-existing namespace.
  # The gateway discovers sandboxes across namespaces via cluster-wide watch.
  # Ref: https://github.com/NVIDIA/OpenShell/blob/main/rfc/0011-multi-player-design/README.md
  drivers:
    kubernetes:
      workspaceMode: operator
