# Plain-text template — NEVER commit this file with real values.
# Run the commands below to produce sealed-loan-agent-auth.yaml, then commit that.
#
# Usage:
#   kubeseal --fetch-cert \
#     --controller-namespace sealed-secrets \
#     --controller-name sealed-secrets-controller \
#     > /tmp/ss-cert.pem
#
#   kubectl create secret generic loan-agent-auth \
#     --namespace loan-agent \
#     --from-literal=llm-api-key="<MAAS_API_KEY>" \
#     --dry-run=client -o yaml \
#   | kubeseal --cert /tmp/ss-cert.pem --format yaml \
#     > ../sealed-loan-agent-auth.yaml
#
#   rm /tmp/ss-cert.pem
apiVersion: v1
kind: Secret
metadata:
  name: loan-agent-auth
  namespace: loan-agent
type: Opaque
stringData:
  llm-api-key: "REPLACE_WITH_MAAS_API_KEY"
