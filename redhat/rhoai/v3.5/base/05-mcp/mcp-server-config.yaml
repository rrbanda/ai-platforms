apiVersion: v1
kind: ConfigMap
metadata:
  name: openshift-mcp-server-config
  namespace: openshift-mcp-server
data:
  config.toml: |
    port = "8080"
    read_only = true
    toolsets = ["pods", "events", "namespaces", "nodes", "configmaps", "logs"]

    [telemetry]
    enabled = false
