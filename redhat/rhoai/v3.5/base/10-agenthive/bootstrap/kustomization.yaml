apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # gitops-subscription, sealed-secrets, and agent-sandbox-crd are already
  # managed by the RHOAI platform (setup/bootstrap/ and 00-operators/).
  # Only include the agenthive-specific patches here.
  # - gitops-subscription.yaml
  # - sealed-secrets.yaml
  # - agent-sandbox-crd.yaml
  - datasciencecluster-patch.yaml
  - dashboard-config-patch.yaml
