apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: loan-agent-allow-router
  namespace: loan-agent
spec:
  podSelector:
    matchLabels:
      app: loan-agent
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: openshift-ingress
    ports:
    - port: 8080
      protocol: TCP
