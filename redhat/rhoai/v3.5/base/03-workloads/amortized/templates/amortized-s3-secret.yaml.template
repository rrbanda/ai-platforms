# DO NOT COMMIT this file. Seal it with kubeseal then commit only the sealed output.
#
# Usage:
#   kubeseal --fetch-cert \
#     --controller-name=sealed-secrets-controller \
#     --controller-namespace=sealed-secrets > /tmp/sealed.pem
#   kubeseal --cert /tmp/sealed.pem --format yaml \
#     < amortized-s3-secret.yaml.template \
#     > ../../../overlays/sandbox1388/amortized/sealed-s3-secret.yaml
#
apiVersion: v1
kind: Secret
metadata:
  name: amortized-s3
  namespace: amortized
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "CHANGE_ME"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  AWS_S3_ENDPOINT: "http://minio.amortized.svc.cluster.local:9000"
  MLFLOW_S3_ENDPOINT_URL: "http://minio.amortized.svc.cluster.local:9000"
  FSSPEC_S3_ENDPOINT_URL: "http://minio.amortized.svc.cluster.local:9000"
  FSSPEC_S3_KEY: "CHANGE_ME"
  FSSPEC_S3_SECRET: "CHANGE_ME"
