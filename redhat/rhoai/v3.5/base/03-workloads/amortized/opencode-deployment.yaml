apiVersion: apps/v1
kind: Deployment
metadata:
  name: opencode
  namespace: amortized
  labels:
    app: amortized
    component: opencode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: amortized
      component: opencode
  template:
    metadata:
      labels:
        app: amortized
        component: opencode
    spec:
      serviceAccountName: amortized-server
      securityContext:
        runAsNonRoot: true
      initContainers:
        - name: build-skills-tree
          image: busybox:1.37
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          command: ["sh", "-c"]
          args:
            - |
              set -e
              cd /flat
              for f in *; do
                [ "$f" = "*" ] && continue
                dir="/skills/$(echo "$f" | sed 's|__|/|g' | xargs dirname)"
                name="$(echo "$f" | sed 's|__|/|g' | xargs basename)"
                mkdir -p "$dir"
                cp "$f" "$dir/$name"
              done
          volumeMounts:
            - name: morty-skills
              mountPath: /flat
              readOnly: true
            - name: skills-tree
              mountPath: /skills
      containers:
        - name: opencode
          image: ghcr.io/anomalyco/opencode:latest
          imagePullPolicy: Always
          command: ["opencode", "serve", "--port", "4096", "--hostname", "0.0.0.0"]
          workingDir: /app/workspace
          ports:
            - name: http
              containerPort: 4096
          env:
            - name: HOME
              value: /tmp
            - name: NODE_TLS_REJECT_UNAUTHORIZED
              value: "0"
            - name: MAAS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: opencode-maas
                  key: maas-token
          volumeMounts:
            - name: opencode-config
              mountPath: /app/workspace/opencode.json
              subPath: .opencode.json
              readOnly: true
            - name: morty-prompt
              mountPath: /app/workspace/.opencode/agents/morty.md
              subPath: morty.md
              readOnly: true
            - name: morty-prompt
              mountPath: /app/workspace/.opencode/agents/sdg.md
              subPath: morty-sdg-workflow.md
              readOnly: true
            - name: morty-prompt
              mountPath: /app/workspace/.opencode/agents/training.md
              subPath: morty-training-workflow.md
              readOnly: true
            - name: skills-tree
              mountPath: /app/workspace/skills
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
      volumes:
        - name: opencode-config
          configMap:
            name: opencode-config
        - name: morty-prompt
          configMap:
            name: morty-config
        - name: morty-skills
          configMap:
            name: morty-skills
        - name: skills-tree
          emptyDir: {}
