apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/09-openshell-dashboard
  - sealed-oauth2-secret.yaml

patchesStrategicMerge:
  - oauth2-proxy-config-patch.yaml
