apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSAuthPolicy
metadata:
  name: REPLACE_WITH_POLICY_NAME
  namespace: models-as-a-service
spec:
  groups:
    - "REPLACE_WITH_GROUP_NAME"
  models:
    - "REPLACE_WITH_MODEL_NAME"
